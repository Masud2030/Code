# Code
// Positive or negative number check.
#include<stdio.h>
int main()
{
int x;
scanf("%d",&x);
if(x>0)
{
printf("%d is a positive number",x);
}
else
{
printf("%d is a negative number",x);
}
return 0;
}
